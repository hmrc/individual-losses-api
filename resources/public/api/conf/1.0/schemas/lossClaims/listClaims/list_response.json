{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "List of Loss Claims",
  "description": "List of Loss Claims",
  "type": "object",
  "properties": {
    "claims": {
      "type": "array",
      "items": {
        "type": "object",
        "description": "Array of loss claims",
        "properties": {
          "id": {
            "description": "The unique identifier of the loss claim",
            "type": "string",
            "pattern": "^[A-Za-z0-9]{15}$",
            "example": "A2Za0911235813z"
          },
          "links": {
            "type": "array",
            "description": "A list of endpoint links that indicate possible actions related to the current item in the list",
            "items": {
              "$ref": "#/definitions/link"
            }
          }
        },
        "required": [
          "id"
        ]
      }
    },
    "links": {
      "type": "array",
      "description": "A list of endpoint links that indicate possible actions related to the current resource",
      "items": {
        "$ref": "#/definitions/link"
      }
    }
  },
  "required": [
    "claims"
  ],
  "definitions": {
    "link": {
      "type": "object",
      "properties": {
        "href": {
          "description": "The relative url of the endpoint",
          "type": "string",
          "example": "/individuals/losses/{nino}/loss-claims/{claimId}"
        },
        "rel": {
          "description": "A label for the endpoint, which describes how it is related to the current resource\nThe rel will be `self` where the action is retrieval of the same resource",
          "type": "string",
          "enum": [
            "self",
            "create-loss-claim",
            "delete-loss-claim",
            "amend-loss-claim"
          ]
        },
        "method": {
          "description": "The HTTP method type for the endpoint",
          "type": "string",
          "enum": [
            "GET",
            "DELETE",
            "POST"
          ]
        }
      },
      "required": [
        "href",
        "rel",
        "method"
      ]
    }
  }
}